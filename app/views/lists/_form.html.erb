<%= form_with(model: [list], class: 'list__form') do |f| %>
  <%= f.text_field :name, 
    placeholder: 'Name', 
    class: "input",
    autofocus: true
  %>

  <%= f.submit 'Salvar', class: 'button' %>
<% end %>
